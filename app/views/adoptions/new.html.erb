<h1>Envoyez un message à <%= @pet.user.first_name %></h1>

<p>Attention ! Adopter un animal est un acte responsable !</p>

<%= simple_form_for [@pet, @adoption] do |f| %>
  <%= f.input :adoption_message, label: "Votre message", input_html: { class: "input-field" }, label_html: { class: 'label-field' } %>
  <%= f.submit "Envoyer à #{@pet.user.first_name}", class: "btn btn-log" %>
<% end %>
