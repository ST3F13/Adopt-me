
<div class="message-container" id="message-<%= message.id %>">
  <i class="author">
    <span><%= message.user.username %></span>
    <small><%= l(message.created_at, :format => '%d %B %Y at %l:%M%p') %></small>
  </i>
  <p><%= message.content %></p>
</div>

